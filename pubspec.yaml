name: gestion_solicitudes_clinicas
description: App Flutter para gestionar solicitudes de clínicas a TI
version: 1.0.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^1.10.5
  file_picker: ^6.1.1
  provider: ^6.1.2
  pdf: ^3.10.8
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
