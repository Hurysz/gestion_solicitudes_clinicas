name: gestion_solicitudes_clinicas
description: App Flutter para gestionar solicitudes de clínicas a TI
version: 1.0.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^1.10.5
  file_picker: ^6.1.1
  provider: ^6.1.2
  cupertino_icons: ^1.0.6
  path: ^1.9.0
  mime: ^1.0.4
  intl: ^0.19.0           # 📅 Para formatear fechas elegantes
  url_launcher: ^6.2.5   # 🔗 Para abrir enlaces de archivos adjuntos
  path_provider: ^2.0.15
  open_file: ^3.3.2
  pdf: ^3.10.4
  share_plus: ^7.2.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
